⛔ FAIL(로컬 QA): 배포 환경 접촉 증거가 0건이다 — localhost·격리 worktree 만으로는 QA 가 아니다(회장 2026-08-30).
   → 반려. 그 벤처의 stage 또는 운영 호스트를 실제로 쳐서 응답·화면 증거를 남긴 뒤 재산출.
   호스트 목록 정본: /Users/sj/.claude/harness/deploy-hosts.tsv (도메인이 빠져 있으면 스크립트가 아니라 이 파일에 줄을 추가하라)
