# 회장 요청 원문 대조: 학습 정보, 생성실, 편집실, 발행실

판정 기준은 `wiki/거버넌스/요청.md` 44~120행의 원문과 현재 제품 코드다. `반영`은 화면과 동작이 코드에 모두 있는 경우, `부분`은 일부만 있거나 직접 관찰 전인 경우, `미반영`은 반대 동작이 코드에 남은 경우다.

## 학습 정보

| 요청 원문 | 현재 화면 상태(코드 근거 파일:줄) | 판정 | 이번 판에서 할 것 |
|---|---|---|---|
| 학습 정보 미입력 상태면 로그인시 팝업으로 뜨던가해서 유도해야하지 않을까. 챗봇에서 물어보던가 | 첫 진입부터 헤더에 `학습 정보 0 / 8 남은 8칸 이어 채우기`가 보이며 누르면 문답이 열린다. 헤드리스 클릭 뒤 본문이 482자 늘었다. `dashboard/src/components/shared/RoomHeader.tsx:113`, `dashboard/src/components/studio/LearningStatus.tsx:37` | 반영 | 헤더 상시 유도와 클릭 문답 진입을 구현하고 실측했다. |
| 무엇을 하는 곳입니까’ 가 무슨 말이냐? 업종을 고르라는거냐? | `어떤 업종에서 일하시나요?`, `사업 분야를 고르세요`로 질문과 대상을 명시한다. `dashboard/src/components/studio/LearningCardWizard.tsx:23` | 반영 | 기존 카드 선택 흐름을 보존하고 클릭으로 재검증한다. |
| ‘누구에게 말합니까’ 가 무슨말이냐? 고객을 말하는거야 우리 유저를 말하는거야 | `우리 서비스를 이용하거나 구매할 고객`이라고 범위를 밝힌다. `dashboard/src/components/studio/LearningCardWizard.tsx:24` | 반영 | 기존 문구와 카드 선택을 보존한다. |
| ‘어떤 결로 말합니까’ 나오는건 예시냐? | `실제 콘텐츠에 적용된 예시`라고 밝힌다. `dashboard/src/components/studio/LearningCardWizard.tsx:25` | 반영 | 기존 예시 카드 흐름을 보존한다. |
| 어떤 결로 말합니까 다음에 완료도 없고 왜 일정 시간 지나면 창 닫혀버리지? | 7개 질문 뒤 검토 화면과 `학습 정보 저장` 단추가 있고 자동 닫힘 타이머가 없다. `dashboard/src/components/studio/LearningCardWizard.tsx:146` | 반영 | 정상 저장과 미완성 닫기를 계약 테스트로 유지한다. |
| 3개 채웠는데 학습정보 누르면 3/8 임. 나머지는 어디서채우라는건지 이해가 안되는 UI인데 | 헤더가 `남은 5칸 이어 채우기`까지 직접 표시하고 전체가 문답 진입 단추다. `dashboard/src/components/studio/LearningStatus.tsx:37` | 반영 | 남은 칸 수와 행동을 보이는 문구로 추가하고 클릭 전후 본문 길이를 측정했다. |
| 나중에 하기 뒤에 헤더 학습 정보에서 이어 채웁니다 이건 뭐냐? | `언제든 헤더의 학습 정보에서 계속할 수 있습니다`로 고쳤다. `dashboard/src/components/studio/LearningCardWizard.tsx:149` | 반영 | 헤더 단추와 실제 연결을 브라우저에서 확인한다. |
| 앞 걸음으로는 뭔 어디에서 벤치마킹한 UX 라이팅이야 ux_wrighting.md 정의 안해놨냐 설마. UX라이팅 및 UI가 거의 쓰레기 수준인데 프로덕션 서비스 만들 생각 없는거임? 벤치마킹 좀만해도 이정도 쓰레기 퀄리티는 안나올듯한데. | `앞 걸음으로`는 제거했고 단추는 `이전`, `다음`, `저장`의 표준 행동어를 쓴다. `dashboard/src/components/studio/LearningCardWizard.tsx:143` | 반영 | Canva의 선택 후 결과 비교, Buffer의 생성 후 편집 진입 구조와 대조하고 모호한 행동어가 없는지 재검증한다. |

## 생성실

| 요청 원문 | 현재 화면 상태(코드 근거 파일:줄) | 판정 | 이번 판에서 할 것 |
|---|---|---|---|
| 내가 영상을 만들지 카드뉴스를 만들지 글을 만들지 어떻게 알고 ‘영상을 이런걸로 만들어 드립니다’ 이지랄임? 그리고 저 결 3개가 대체 뭘 말하는건지도 모르겠고 뭔 생각으로 저렇게 분리한지도 모르겠음 | 첫 상태는 `콘텐츠 구성 초안 예시`이고 영상으로 단정하지 않는다. A, B, C에 문제 제시형, 결과 제시형, 과정 설명형과 순서를 표시한다. `dashboard/src/components/studio/StudioRooms.tsx:327` | 반영 | 형식 미선택 상태와 구조 설명을 보존한다. |
| 챗봇의 의미가 채팅아니냐? 그럼 하나씩 대화하면서 입력받아야하는거 아니야? 정보를 그냥 다 나열을 해놨네 병신 | 현재 질문 하나만 `data-create-question`으로 렌더하고 답한 뒤 다음 질문으로 이동한다. `dashboard/src/components/studio/StudioRooms.tsx:464` | 반영 | 한 시점에 한 질문만 보이는 계약을 유지하고 클릭으로 재검증한다. |
| 만들 종류는 영상 카드뉴스 글 선택하게끔 했는데 이 주제로 같이 만들 것은 왜 또 중복으로 있는거지? | 만들 형식 질문 하나에서 주 형식과 추가 형식을 함께 고른다. 중복 문구는 없다. `dashboard/src/components/studio/StudioRooms.tsx:465` | 반영 | 기존 다중 선택을 보존한다. |
| ‘이번에 노리는 것’ ‘말 거는 대상’ 이 뭔 UX라이팅인지 이해가 안되네. 알리기, 믿게 하기… 아주 문장이 좆같네 | `이번 콘텐츠로 원하는 결과`, `누가 이 콘텐츠를 보나요`로 바뀌었다. `dashboard/src/components/studio/StudioRooms.tsx:476` | 반영 | 현재 질문 문구를 보존한다. |
| 주제에서 저게 최선의 선택지 질문이냐? | 업종과 목표를 바탕으로 세 주제를 제안하고 `직접 입력` 탈출구를 둔다. `dashboard/src/components/studio/StudioRooms.tsx:495` | 반영 | 제안과 직접 입력이 모두 동작하는지 유지한다. |
| 생성실 챗봇인데 학습 정보 더 채우기 버튼은 왜 있는 것이며… | 생성 담당 안에는 학습 정보 단추가 없고 학습 정보는 헤더가 소유한다. `dashboard/src/components/studio/StudioRooms.tsx:451` | 반영 | 생성 담당에 학습 정보 단추를 다시 넣지 않는다. |
| 임시저장이 왜 일부만 되는거지 생성실에서 새로고침하면 리셋되는게 상식아닌가 | 주제는 작업 공간 저장에서 항상 복원하고, 형식·질문 위치·구조·후보도 별도 작업 공간 키에 임시 저장한다. 헤드리스 새로고침 뒤 `새로고침 뒤에도 남는 고객 질문`이 그대로 남았다. `dashboard/src/app/studio/page.tsx:356`, `dashboard/src/components/studio/StudioRooms.tsx:265` | 반영 | 주제, 형식, 질문 위치, 구조 선택과 생성 결과를 작업 공간별로 복원하고 깨진 저장값은 폐기하게 했다. |
| A, B, C 누르면 영상 후보 안나오는데? 그리고 카드뉴스 글도 선택했는데 그건 안보여주는거야? | A, B, C를 누르면 선택 구조 후보가 즉시 나오고, 생성 뒤에는 고른 영상·카드뉴스·글 후보를 형식별 카드로 모두 보여 준다. `dashboard/src/components/studio/StudioRooms.tsx:473`, `dashboard/src/components/studio/StudioRooms.tsx:547` | 반영 | 고른 형식별 후보를 구분해 표시하고 고르지 않은 형식 혼입을 거절 테스트로 막았다. |
| ‘선택한 구조 초안을 편집실로 옮길 수 있습니다. 실제 미디어 생성은 준비 중입니다.’ 아래에 보이는, 확정하고 같이 만들기’ 쪽 CTA나 소개 아주 개좆같아. 뭘하자는건지도 모르겠고. 편집실로 이동을 해야하는거야 확정하고 같이 만들기를 해야하는거야 아주 UX가 좆같구나 | 구조 선택 뒤 주 행동은 `편집실에서 다듬기` 하나이고 문제 문구와 `확정하고 같이 만들기`는 없다. `dashboard/src/components/studio/StudioRooms.tsx:552` | 반영 | 단일 주 행동을 보존한다. |
| ‘생성 저장소의 무결성 조건을 확인하지 못했습니다’ 이건 대체 뭔 에러? | 내부 오류를 `구조 초안을 저장하지 못했습니다. 잠시 후 다시 시도해 주세요`로 바꾼다. `dashboard/src/components/studio/StudioRooms.tsx:134` | 반영 | 오류 변환 계약을 유지한다. |

## 편집실

| 요청 원문 | 현재 화면 상태(코드 근거 파일:줄) | 판정 | 이번 판에서 할 것 |
|---|---|---|---|
| 편집 기능안만들었냐? 편집으로 넘어가도 뭘 바꾸는건지 전혀 감이 안잡히네 | 머리에 `내용과 화면을 직접 다듬습니다`와 형식, 채널의 소유 경계를 표시한다. `dashboard/src/components/studio/StudioRooms.tsx:775` | 반영 | 실제 편집 전후 값을 헤드리스로 측정한다. |
| 글하고 카드뉴스밖에 선택 안되는 이유는 뭐냐? | 글, 카드뉴스, 영상, 음악 네 형식을 함께 보여 준다. `dashboard/src/components/studio/StudioRooms.tsx:778` | 반영 | 네 형식 단추를 보존한다. |
| 카드뉴스는 이미지 내에서 텍스트 수정할 수 있게끔하거나 드래그로 이동시키게끔하는게 맞지 않겠냐? | 카드 캔버스 안에 글자 입력과 `카드 글자 끌어 옮기기` 손잡이가 있다. `dashboard/src/components/studio/EditPreview.tsx:178` | 반영 | 입력값과 포인터 이동을 계약 테스트 및 브라우저로 재검증한다. |
| 카드뉴스 선택했는데 스레드 X FACEBOOK은 뭔 옵션임? | 편집실에서 플랫폼 선택을 제거하고 `형식과 채널은 다릅니다`라고 밝힌다. `dashboard/src/components/studio/StudioRooms.tsx:794` | 반영 | 편집실에 플랫폼 선택이 없는지 유지한다. |
| 플랫폼 선택은 뭐고 사진크기 선택은 뭐지 작업실 책상은 뭐고 ‘보통’ 은 뭘 보통 작게 크게를 말하는거임? | `카드 비율`, 픽셀 크기, `배경 이미지`, `카드 글자 크기`처럼 단위와 대상을 붙인다. `dashboard/src/components/studio/StudioRooms.tsx:653` | 반영 | 명확한 라벨을 보존한다. |
| ‘여기서만 한 번에 되는일’ 은 대체 뭔 설명이고 | 발행 담당의 남은 문구도 `여러 채널 함께 바꾸기`로 바꿔 대상과 행동을 직접 말한다. `dashboard/src/app/studio/page.tsx:1332` | 반영 | 모호한 문구를 제거했다. |
| 글 선택했을때 자막고치듯이 저렇게 한 이유는 뭘까 대체 뭔 판단임? | 글은 초 단위 대사 목록 대신 `글 전체 편집` 문서형 입력창을 쓴다. `dashboard/src/components/studio/StudioRooms.tsx:811` | 반영 | 이전 배포의 문서형 편집을 보존하고 값 반영을 실측한다. |
| 다음단계로 넘어가려면 뭔 버튼을 눌러야할지도 모르겠고 편집 작업물로 저장을 누르면 되는거냐? 편집실인데 편집실 작업물에 추가를 왜 하는건지도 모르겠고 그다음 발행실로 어떻게 넘어가는지도 모르겠고 | 유일한 주 행동이 `발행실로 이동`이며 자동 저장 상태를 함께 보여 준다. `dashboard/src/components/studio/StudioRooms.tsx:895` | 반영 | 발행실 이동 단추와 자동 저장을 보존한다. |

## 발행실

| 요청 원문 | 현재 화면 상태(코드 근거 파일:줄) | 판정 | 이번 판에서 할 것 |
|---|---|---|---|
| 플랫폼 별로 표시이름, 캡션, 해시태그 이런거 조사하고 실제에 맞게 고치라고 했는데 “개무시한 이유” 분석좀해라. 표시 이름이 뭔데 이걸 내가 수정을 할 수가 있는거냐? 씨발년이 요청을 기록을 안해놓냐? 요청과 대조안해? 작업을 대체 뭔 프로세스로 하길래 자꾸 누락하는게 있는거지 장애년인가? | 표시 이름과 사용자명은 OAuth 연결 계정의 읽기 전용 값이며 캡션과 해시태그는 플랫폼 계약에 따라 편집한다. `dashboard/src/components/studio/PlatformPreview.tsx:101` | 반영 | 계정 값과 게시물 값의 소유 경계를 보존한다. |
| 글은 왜 편집이 안돼? 이전에 배포했던 화면 또 개무시했지? 안 읽냐 이전 산출물들을? | v65에서 복원한 `글 전체 편집` 입력창이 현재 코드와 커밋 이력에 남아 있다. `dashboard/src/components/studio/StudioRooms.tsx:811` | 반영 | 이전 배포 방식을 유지하고 변경 전후 값을 실측한다. |
| 승인 인박스로 보내기가 뭔소린지도 모르겠고 운영자 API 라는데 뭔말임 | 사용자 행동은 `검토 요청하기`, 목적지는 `검토 대기`로 바뀌었고 내부 API 문구는 없다. `dashboard/src/app/studio/page.tsx:1198` | 반영 | 현재 표준 용어를 보존한다. |

## 2026-08-29 원문 중 같은 화면에 이어진 항목

| 요청 원문 | 현재 화면 상태(코드 근거 파일:줄) | 판정 | 이번 판에서 할 것 |
|---|---|---|---|
| 보니까 채팅에서 ‘학습 정보 채우기’ 누르니까 U2, U3 을 받고 있는 것 같은데 이걸 이렇게 위계 두는게 맞냐? 첫 화면에서 팝업이든 챗봇에서든 받고, 닫으면 헤더의 학습 정보를 잠깐 깜빡거리게 하던지 해서 여기서 채울 수 있다고 안내하는 식이 낫지 않겠냐? 왜 헤더에 학습 정보가 사라짐? 그리고 이렇게 만드는게 ‘브랜드 가이드’ 맞아? | 헤더에 남은 칸과 이어 채우기 행동을 상시 노출하고, 덜 채운 채 닫으면 한 번 깜빡이며 같은 단추가 문답을 연다. `dashboard/src/components/shared/RoomHeader.tsx:113`, `dashboard/src/components/studio/LearningStatus.tsx:16` | 반영 | 첫 진입부터 보이는 헤더 유도, 재진입, 한 번 깜빡임을 고정했다. |
| 우리 목표가 프롬프팅이나 하네스 엔지니어링없이 백지공포 기술공포 없이 클릭 클릭으로 컨텐츠를 만들어 관리하는 에이전트라는게 핵심인데 R6 정보도, 학습정보에서 받는 U2 U3 정보도 주관식이면 나라도 뭘 입력해야할 지를 모르겠는데 | 학습 정보 7개와 생성실 형식, 목표, 고객, 주제는 카드 선택이며 직접 입력은 탈출구로만 남아 있다. `dashboard/src/components/studio/LearningCardWizard.tsx:23`, `dashboard/src/components/studio/StudioRooms.tsx:465` | 반영 | 카드 선택 기본 흐름을 보존한다. |
| ‘오늘 만들 수 있는 것’ 은 뭐하는 예시이지? 추천 영상이야? 전혀 모르겠어. 만들 수 있는게 저 3종류밖에 있는게 아닌데. | 문제 문구는 제거했고 `콘텐츠 구성 초안 예시`와 현재 제공, 준비 중 범위를 분리했다. `dashboard/src/components/studio/StudioRooms.tsx:327` | 반영 | 제공 범위 문구를 유지한다. |
| 지금 이 폴더에서 영상 카드뉴스 등 실험중인데 카드뉴스에서는 브랜딩 인아웃트로, 후킹, CTA 같은거 고민중이고 카드뉴스에 메인 사진, 본문, 클로징 사진 등 있는데 이런걸 후보군에서 고르면서 이런 결로 만들고 싶다 결정해야하지 않을까 싶어 하나의 제안이야. 벤치마킹 잘 해봐. | A, B, C 구조와 카드 슬라이드 문구는 있으나 메인 사진, 본문, 마무리 사진의 시각 후보 선택은 아직 없다. `dashboard/src/components/studio/StudioRooms.tsx:416`, `dashboard/src/components/studio/EditPreview.tsx:173` | 부분 | 이번 판 범위인 형식별 실제 텍스트와 카드 슬라이드 후보 표시까지 구현한다. 사진 후보 생성과 선택은 미검증 범위로 남긴다. |

## 대조 요약

- 전건: 33건
- 착수 시 판정: 반영 27건, 부분 4건, 미반영 2건
- 최종 판정: 반영 32건, 부분 1건, 미반영 0건
- 이번 판 구현: 첫 진입부터 보이는 헤더 학습 정보 유도, 남은 칸 행동, 생성실 새로고침 복원, 형식별 생성 후보 표시, 남은 모호한 발행 담당 문구 교정
- 범위 밖 미검증: 카드뉴스 사진 후보 자체 생성과 선택, 실제 외부 생성 엔진, 운영 배포 화면
- 직접 관찰: 헤더 학습 정보 문구 확인, 학습 정보 클릭 `+482자`, 구조 B 선택 `+9자`, 새로고침 입력 보존, 글 본문 편집 전후 값 반영, 브라우저 콘솔 오류 0건. 원본은 `docs/qa/v77-click-observations.json`.

STAMP | line: osmu-v77req | 생성: 2026-09-04 07:11 KST | model: gpt-codex/gpt-5.6-sol | agent: code-builder | skill: 없음 | 근거: 회장 요청 원문, ADR-004·005·006, DESIGN v37, 승인 v68 프로토타입, 현재 코드, Canva와 Buffer 공식 문서, 헤드리스 브라우저 실측 | 고민: 기존 편집·발행 기능은 보존하고 요청과 코드가 어긋난 자리만 고쳤다. 헤드리스 첫 시도에서 접힘 메뉴 안의 학습 정보를 발견해 상시 노출로 수정했다.

SKILLS_USED: 없음

SKILLS_SKIPPED: 이 기존 Next.js 제품의 증분 구현과 직접 일치하는 설치 스킬이 없음

SOURCES: `wiki/거버넌스/요청.md` 44~120행 · `wiki/거버넌스/결정.md` ADR-004·005·006 · `wiki/거버넌스/실수.md` 상단 · `pipeline-state.osmu.md` v68 build 핀 · `DESIGN.md` v37 · `docs/prototype/osmu-v68-create-performance-hub-gpt-codex-20260903-0022.html` · 현재 `dashboard/src` 코드 · https://www.canva.com/help/use-magic-design/ · https://support.buffer.com/en-us/articles/using-buffers-ai-assistant-qbBulfw9sn

MODEL: gpt-codex/gpt-5.6-sol
